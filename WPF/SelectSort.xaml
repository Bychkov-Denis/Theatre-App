<Window x:Class="WPF.SelectSortModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF"
        mc:Ignorable="d"
        Title="SelectSort" Height="160" Width="150">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <RadioButton Grid.Row="0" GroupName="sort" IsChecked="False" Name="ascending">По алфавиту</RadioButton>
		<RadioButton Grid.Row="1" GroupName="sort" IsChecked="False" Name="descending">Обратный порядок</RadioButton>
        <Button Grid.Row="3" Name="save">Сохранить</Button>
    </Grid>
</Window>
